import { Rating } from './types';

const rating: Rating = {
    active: {
        DEFAULT: {
            BASE: '',
            primary: 'fill-[#FACC15]'
        }
    },
    empty: {
        DEFAULT: {
            BASE: '',
            primary: 'fill-[#D1D5DB]'
        }
    }
};

export default rating;
