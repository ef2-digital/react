import { Copyright } from './types';

const copyright: Copyright = {
    variant: {
        DEFAULT: {
            DEFAULT: 'inline-flex items-center hover:underline',
            primary: 'text-white'
        }
    }
};

export default copyright;
