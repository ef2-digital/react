import { Meta, Story, Canvas, Source, ArgsTable, IconGallery, IconItem } from '@storybook/addon-docs';
import ThemeProvider from '.';
import Button from '../Button';
import Rating from '../Rating';
import Copyright from '../Copyright';
import dedent from 'ts-dedent';

<Meta title="ThemeProvider" component={ThemeProvider} />

# ThemeProvider

export const Template = (args) => (
    <ThemeProvider {...args}>
        <div className="flex mb-6 items-center">
            <Button className="mr-3">Button</Button>
            <Rating value={3} />
        </div>
        <Copyright />
    </ThemeProvider>
);

export const ButtonTemplate = (args) => (
    <ThemeProvider {...args}>
        <Button>Button</Button>
    </ThemeProvider>
);

export const LanguageTemplate = (args) => (
    <ThemeProvider {...args}>
        <Copyright>Button</Copyright>
    </ThemeProvider>
);

<Canvas>
    <Story name="Default">{Template.bind({})}</Story>
</Canvas>

```tsx
// components/layout/DefaultLayout.tsx
import { ThemeProvider } from '@ef2-digital/react';
import theme from 'theme'; // your custom theme :D.

const DefaultLayout = ({ children }: PropsWithChildren) => {
    return (
        <ThemeProvider theme={theme}>
            <main id="main">{children}</main>
        </ThemeProvider>
    );
};
```

### Button
```tsx
// theme.ts
import { PartialTheme } from '@ef2-digital/react';

const theme: PartialTheme = {
    button: {
        variant: {
            DEFAULT: {
                BASE: 'transition-shadow rounded-none shadow-[3px_3px_0_0] hover:shadow-[4px_4px_0_0] focus:shadow-[4px_4px_0_0]',
                primary: 'shadow-indigo-800 hover:shadow-indigo-900 focus:shadow-indigo-900'
            }
        }
    }
};

export default theme;
```
<Story
    name="Primary"
    args={{
        theme: {
            button: {
                variant: {
                    DEFAULT: {
                        BASE: 'transition-shadow rounded-none shadow-[3px_3px_0_0] hover:shadow-[4px_4px_0_0] focus:shadow-[4px_4px_0_0]',
                        primary: 'shadow-indigo-800 hover:shadow-indigo-900 focus:shadow-indigo-900'
                    }
                }
            }
        }
    }}
>
    {ButtonTemplate.bind({})}
</Story>

### Locale
<Story
    name="Locale"
>
    <ThemeProvider lng="nl">
        <Copyright/>
    </ThemeProvider>
</Story>
